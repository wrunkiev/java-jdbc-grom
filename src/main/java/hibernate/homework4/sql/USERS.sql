CREATE TABLE USERS(
ID NUMBER NOT NULL ,
USER_NAME NVARCHAR2(50) NOT NULL,
USER_PASSWORD NVARCHAR2(50),
USER_COUNTRY NVARCHAR2(50),
USER_TYPE_ID NVARCHAR2(50),
CONSTRAINT USER_ID PRIMARY KEY(ID),
CONSTRAINT USER_TYPE_FK FOREIGN KEY(USER_TYPE_ID) REFERENCES TYPES(ID)
);






















